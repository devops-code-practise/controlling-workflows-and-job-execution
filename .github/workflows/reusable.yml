name: Reusable Deploy
# now it can be called inside other workflows
on: workflow_call
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: echo "Deploying and uploading..."
